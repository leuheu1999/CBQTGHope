@using CMS.Admin.Common
@model Business.Entities.Domain.DM_VungMienMapAdd
<div class="hidden">
    @Html.HiddenFor(model => model.Id)
</div>
<div>
    <div class="form-group">
        <label class="font-weight-bold">Mã <span class="text-danger font-weight-bold">(*)</span></label>
        @Html.TextBoxFor(model => model.Ma, new { @class = "form-control form-control-sm", @placeholder = "Mã" })
        <div class="field-validation-error thongbaomaVungMien invalid-feedback"></div>
    </div>
    <div class="form-group">
        <label class="font-weight-bold">Tiền tố <span class="text-danger font-weight-bold">(*)</span></label>
        @Html.TextBoxFor(model => model.Prefix, new { @class = "form-control form-control-sm", @placeholder = "Tiền tố" })
        <div class="field-validation-error thongbaotientoVungMien invalid-feedback"></div>
    </div>
    <div class="form-group">
        <label class="font-weight-bold">Tên vùng miền <span class="text-danger font-weight-bold">(*)</span></label>
        @Html.TextBoxFor(model => model.TenVungMien, new { @class = "form-control form-control-sm", @placeholder = "Tên vùng miền" })
        <div class="field-validation-error thongbaotenVungMien invalid-feedback"></div>
    </div>
    <div class="form-group">
        <label class="font-weight-bold">Mô tả</label>
        @Html.TextAreaFor(model => model.MoTa, new { @class = "form-control form-control-sm", @placeholder = "Nhập mô tả" })
        <div class="field-validation-error thongbaomotaVungMien invalid-feedback"></div>
    </div>
    <div class="form-group form-check mt-3">
        <input type="checkbox" class="form-check-input" name="IsActive" id="check-is-active" value="@(Model.IsActive == true ? "True" : "False")" @(Model.IsActive == true ? "checked='checked'" : "")>
        <label class="form-check-label font-weight-bold" for="check-is-active">Hoạt động</label>
    </div>
</div>